COMPONENT=RawAppC
ROOT_DIR= $(COMPRESSIVE_SENSING)/tinyos-2.1.1
SOURCE_ROOT= $(COMPRESSIVE_SENSING)/Blink
#CFLAGS += -DPRINTFUART_ENABLED
CFLAGS +=  -I$(SOURCE_ROOT)/soft_aes  -I$(SOURCE_ROOT)/splug -I$(SOURCE_ROOT) -I$(SOURCE_ROOT)/splug/driver -I$(SOURCE_ROOT)/splug/driver/spi -I$(SOURCE_ROOT)/reliable -I$(SOURCE_ROOT)/raw

# raw transmission, circular buffer
CFLAGS += -DCIRCULAR_BUFFER

# CFLAGS += -DDOWN_SAMPLING

CFLAGS += -DCC2420_DEF_CHANNEL=17

CFLAGS += -DTOSH_DATA_LENGTH=120
include $(MAKERULES)

